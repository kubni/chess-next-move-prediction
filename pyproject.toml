[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "chessml"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["chess", "numpy", "zstandard", "pandas", "matplotlib"]

[tool.setuptools.packages.find]
include = ["chessml*"]
